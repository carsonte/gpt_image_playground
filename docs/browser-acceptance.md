# 浏览器级验收记录

更新时间：2026-08-29

本记录用于发布前在本机 Google Chrome 中重复验收。测试只使用本地 Vite、应用服务器和 mock 上游，不上传用户图片或生产密钥。

## 启动方式

在项目根目录分别启动以下服务：

```bash
node scripts/mock-image-api.mjs
npm run server
npm run dev
```

托管版本地验收使用与 `scripts/dev-full.mjs` 相同的 Vite 环境变量，将 API 代理指向本地应用服务器；mock 上游监听 `127.0.0.1:8787`，应用服务器监听 `127.0.0.1:8788`，Vite 监听 `127.0.0.1:5173`。端口被占用时可使用等价的空闲端口，并同步修改代理地址。

本次功能验收另外使用了隔离的本地环境：成功 mock 服务器 `8798`、500 错误 mock 服务器 `8799`，对应 Vite 页面为 `5183` 和 `5184`。两个环境只使用临时测试密钥和临时数据库目录，不连接生产上游。

## 可重复步骤与结果

| 场景 | 操作 | 结果（2026-08-29） |
| --- | --- | --- |
| 首页加载 | 打开 `http://127.0.0.1:5173/`，等待页面稳定 | 通过；标题、顶部队列状态、输入栏和画廊均正常显示 |
| 主题与移动视口 | 切换深色/浅色主题，并在移动 viewport 重新加载 | 通过；主题立即切换，移动布局无横向溢出，初始化无闪烁错误 |
| 设置弹窗 | 点击“设置”，检查弹窗内容，再关闭 | 通过；可打开、关闭，未提前加载后台模块 |
| 输入栏 | 在提示词输入框填写文本，清空并重新填写 | 通过；输入、清空按钮和生成按钮状态正确 |
| mock 生成 | 使用本地 mock 成功模式提交一次生成 | 通过；生成记录出现成功结果，图片可在画廊查看 |
| mock 编辑 | 从生成结果打开“编辑输出”，修改提示词并提交 | 通过；请求使用 `/images/edits`，编辑记录和结果正常出现 |
| 详情/大图 | 打开一条记录的详情，再打开大图并关闭 | 通过；两个弹窗均可打开和关闭 |
| 错误请求 ID | 使用 mock HTTP 错误模式提交请求 | 通过；错误详情同时显示脱敏错误摘要和 `请求 ID`，后台日志可按 ID 关联 |
| 质量诊断 | 使用 mock 返回 `quality=low`，请求参数为 `high`，在后台按请求 ID 查看 | 通过；生成记录显示“请求质量 high / 实际上游质量 low”，日志 `qualityMismatch=true` |
| `/admin` 独立加载 | 直接打开 `http://127.0.0.1:5173/admin` 并登录 | 通过；后台独立渲染，首页模块未在后台入口前置加载 |
| 后台四组路由 | 在“系统设置”中查看无图生成/有图编辑的 2K/4K 四组列表，上移 CatAPI 并保存 | 通过；列表即时重排，保存后数据库顺序为 `CatAPI → Sixoner → BlackEngine`（仅测试环境） |
| 全站流式开关 | 关闭“允许流式传输”并保存，提交一次生成/编辑请求 | 通过；请求完成，服务端诊断记录 `streamEnabled=false`、`effectiveStream=false`，上游未收到流式字段 |
| 自动不匹配回退 | 打开“质量/尺寸不匹配时自动回退”并保存 | 通过；开关状态持久化，风险提示可见；SSE 仍不执行不匹配重试（由服务端 smoke 覆盖） |
| 恢复推荐设置 | 先修改路由、流式、并发和首页提示，再确认“恢复推荐设置” | 通过；四组恢复为 `Sixoner → CatAPI → BlackEngine`、流式开启、自动回退关闭；并发和首页提示保持原值 |
| 错误请求 ID | 在 `http://127.0.0.1:5184/` 使用 500 mock 提交生成 | 通过；前台显示脱敏错误摘要和请求 ID，未显示密钥或完整请求体 |
| 控制台 | 检查上述页面的 Chrome DevTools 控制台 | 通过；没有新增 error、warning 或未处理 Promise |

## 结果摘要

- 首页与 `/admin` 均可独立加载；入口采用路径动态导入和轻量 `Suspense` 占位。
- 本地 mock 已覆盖一次生成、一次编辑、上游错误和请求 ID 展示。
- 质量诊断可区分浏览器请求参数与上游实际返回参数，并在不保存图片内容的前提下关联请求 ID。
- 后台路由、流式开关和恢复推荐设置在 Chrome 中完成了一次保存/重载验收；设置只影响新请求和等待队列，不中断已运行请求。
- 验收过程中未读取、上传或持久化用户图片，未在浏览器中填写生产密钥。

## 后续复测建议

每次发布前先运行 `npm run verify:release`，再按上表顺序复测首页、编辑、错误请求 ID 和 `/admin`。如需核对网络阶段诊断，在后台“日志中心”按请求 ID 查看线路、阶段耗时、请求/响应字节数和回退次数。
